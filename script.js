let scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(75,1,0.1,1000),renderer=new THREE.WebGLRenderer({canvas:document.getElementById("canvas")});renderer.setClearColor(0x000000,1);let opaqueGeo=new THREE.SphereGeometry(5,24,24),opaqueMat=new THREE.MeshBasicMaterial({color:0x000000,polygonOffset:true,polygonOffsetFactor:1,polygonOffsetUnits:1}),opaqueSphere=new THREE.Mesh(opaqueGeo,opaqueMat);scene.add(opaqueSphere);var wireframeGeo=new THREE.EdgesGeometry(opaqueSphere.geometry),wireframeMat=new THREE.LineBasicMaterial({color:0x660000,linewidth:1}),wireframeSphere=new THREE.LineSegments(wireframeGeo,wireframeMat);opaqueSphere.add(wireframeSphere);camera.position.z=8.5;function animate(t){requestAnimationFrame(animate),opaqueSphere.rotation.y+=0.01,renderer.render(scene,camera);}animate();
